import { Link } from "gatsby"
import React from "react"
import styled from "styled-components"
const StyledHeader = styled.header`
  height: 120px;
`
const Header = ({ siteTitle }) => <StyledHeader>{null}</StyledHeader>

export default Header
